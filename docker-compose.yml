version: '3.8'

services:
  backend:
    build: .
    container_name: cti_backend
    ports:
      - "7860:7860"
    environment:
      - PORT=7860
      - GROQ_API_KEY=${GROQ_API_KEY}
      - GROQ_MODEL=${GROQ_MODEL:-llama-3.1-8b-instant}
    env_file:
      - .env
    restart: always


